# Copy to .env (gitignored): cp .env.example .env
#
# Verbose pipeline logs (normalize tables, enrich per-paper, classified samples).
# Use 1 or true to enable; omit or 0 for cron-friendly output.
DEBUG_NORMALIZED=0
